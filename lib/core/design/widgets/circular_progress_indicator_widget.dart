import 'package:flutter/material.dart';

class WendysCircularProgressIndicator extends StatelessWidget {
  const WendysCircularProgressIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator(
      color: Colors.red,
    );
  }
}
